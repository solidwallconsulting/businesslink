{% extends 'base.html.twig' %}

{% block title %}AttributeValues{% endblock %}

{% block body %}
    <h1>AttributeValues</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attribute_value.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ attribute_value.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('attribute_values_index') }}">back to list</a>

    <a href="{{ path('attribute_values_edit', {'id': attribute_value.id}) }}">edit</a>

    {{ include('attribute_values/_delete_form.html.twig') }}
{% endblock %}
