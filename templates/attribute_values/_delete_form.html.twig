<form method="post" action="{{ path('attribute_values_delete', {'id': attribute_value.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attribute_value.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
