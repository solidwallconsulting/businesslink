 
{% extends 'admin-base.html.twig' %}


{% block body %}


<main class="main-content">
    {% block topNav %}
    {{ parent() }}
    {% endblock %}

    <div class="conatiner-fluid content-inner mt-n5 py-0">
        <div class="row">

            <div class="col-12">
                <div class="card">
                    <div class="card-header d-flex justify-content-between flex-wrap">
                        <div class="header-title">
                            <h4 class="card-title">Affectation d'attributs</h4>
                        </div>

                        <div>

                            <a class="btn btn-primary btn-sm" href="{{ path('attribute_category_new' ) }}">Ajouter</a>
                        </div>

                    </div>
                    <div class="card-body w-100">




                        <table class="table datatable">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Attribut</th>
                                    <th>Categorie</th>
                                    
                                    <th>actions</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for attribute_category in attribute_categories %}
                                <tr>
                                    <td>{{ loop.index }}</td>
                                    <td>{{ attribute_category.attribute }}</td>
                                    <td>{{ attribute_category.category }}</td>
                                    
                                    <td> 
                                        <a href="{{ path('attribute_category_edit', {'id': attribute_category.id}) }}">Modifier</a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="2">Aucun enregistrement trouvé</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>



                    </div>


                </div>

            </div>


            {% endblock %}

