{% extends 'base.html.twig' %}

{% block title %}AttributeCategory{% endblock %}

{% block body %}
    <h1>AttributeCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attribute_category.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('attribute_category_index') }}">back to list</a>

    <a href="{{ path('attribute_category_edit', {'id': attribute_category.id}) }}">edit</a>

    {{ include('attribute_category/_delete_form.html.twig') }}
{% endblock %}
